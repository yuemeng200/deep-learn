# WorkRally

---

## 一、TypeScript 全栈工程基础

1. 你如何在 Koa 中设计错误处理中间件，保证 API 层面的一致性与可观测性？
2. Next.js App Router 和 Pages Router 的核心架构差异是什么？你在什么场景会选择 Server Actions？
3. 描述一个你在生产中解决 TypeScript 类型系统复杂问题的案例，比如 conditional types 或 infer 的实际应用。
4. 如何设计一个跨 Web/桌面/移动端共享核心业务逻辑的 monorepo 架构？包管理和构建策略如何选型？
5. 你在 Electron 或 Tauri 上做过哪些生产级交付？主进程与渲染进程的通信方案如何设计，有哪些踩坑经历？
6. 你在 React Native 或 Flutter 上有过生产级交付吗？跨端调用原生能力（相机、文件系统、推送）的桥接方案怎么设计的？遇到哪些性能瓶颈？
7. 如果让你用一套代码同时覆盖 Web 和移动端，你会选 RN + Next.js 共享逻辑层，还是 Flutter 全包？各自的 trade-off 是什么？

> 准备方向：能说出架构层面的设计决策，而非只有 API 用法；要有真实项目中的权衡经历。即使移动端不是主战场，也要能讲清楚架构选型的判断逻辑。

---

## 二、LLM 核心机制与 API 集成

1. Function Calling 和 Tool Use 的本质机制是什么？如何处理模型返回多个 tool call 的并发场景？
2. Prompt Caching 的命中条件是什么？你在哪些场景下设计过利用缓存降低延迟和成本的方案？
3. SSE 和 WebSocket 流式输出各自适合什么场景？在 LLM 应用中如何处理流式中断和断点续传？
4. Structured Output 有哪些局限性？在模型不遵守格式时你如何做防御？
5. 你如何评估不同模型在特定任务上的表现？会建立什么样的 eval 体系？

> 准备方向：能结合 Anthropic/OpenAI 文档说出底层实现细节，最好有 token 消耗优化的真实数据。

---

## 三、Agent 架构与链路设计

1. 描述你设计过的 Agent Loop 核心结构，如何做 planning、工具选择决策和结果验证？
2. 在 multi-agent 协作中，如何解决子 Agent 之间的上下文隔离与信息传递问题？
3. 当 Agent 执行中途发生错误（工具调用失败、模型幻觉、无限循环），你的错误恢复机制是什么？
4. LangGraph、Mastra、Vercel AI SDK 你用过哪个？讲一个你用编排框架解决实际问题的案例。
5. 如何给 Agent 行为加上可观测性（trace、step log、token 追踪）？用过 LangSmith 或类似工具吗？

> 准备方向：能画出你实现过的 Agent 架构图，讲清楚每个节点的决策逻辑是核心加分项。

---

## 四、MCP / SKILL.md / AG-UI 开放标准

1. MCP 解决了什么问题？和直接定义 Function Calling 工具相比，它的优势在哪？
2. 你有写过或使用过 MCP Server 吗？描述你实现的一个 MCP Tool 的完整生命周期。
3. SKILL.md 体系的核心思想是什么？你认为它与传统 prompt engineering 的本质区别在哪？
4. AG-UI 协议的目标是什么？你如何在产品中实现生成式 UI 的动态渲染？
5. 如果让你在团队内建立一套 SKILL 体系，你会如何设计粒度、命名规范和触发机制？

> 准备方向：MCP 和 SKILL.md 是 JD 高权重项，最好有亲手实现的经历，不能只停留在"知道有这个东西"。

---

## 五、生成式 UI 与端侧 AI 前沿

1. 你如何理解"生成式 UI"？举一个具体产品场景，说明它和传统模板渲染的核心区别。
2. 在 Vercel AI SDK 的 streamUI / generateUI 能力上，你做过哪些探索？遇到了什么工程挑战？
3. 端侧 AI（WebLLM、Transformers.js、Core ML）和云端 API 的选型逻辑是什么？什么场景推端侧？
4. 如果你来定义"下一代创作工具"的形态，AI 应该介入创作流程的哪些节点？怎么设计交互？
5. WorkRally 的三大产品线——短番（短篇漫画）、动画、个人——你觉得 AI 介入创作流程的方式有什么本质差异？请分别描述一个核心 AI 功能的设计思路。
6. 以"短番"为例，从用户打开工具到导出成品，AI 可以介入哪些节点？你会如何设计"人机协作"的交互边界——哪些环节 AI 自动完成，哪些必须用户确认？
7. 动画类产品的 AI 需求和静态内容（短番/图文）有什么不同？实时性、帧一致性、风格一致性这些工程挑战你怎么解决？

> 准备方向：这是偏 vision 的考察，要有产品品味，能从用户体验角度切入，而不只是技术实现。建议提前深入体验 WorkRally 的产品，至少能说出每个产品线的核心用户场景和一个你觉得可以改进的点。

---

## 六、AI-Native 开发与 Harness Engineering

1. 你在日常开发中如何用 Claude Code / Cursor？举一个具体案例说明它如何改变了你的工作流。
2. "把 AI Coding 工具当范式而非辅助"具体体现在哪里？有没有完全由 AI 驱动的端到端交付经历？
3. Harness Engineering 是什么理念？如何向对 AI 工具抵触的工程师推广这套方法论？
4. 你会如何给团队建立 AI 编码的最佳实践文档和工具链？包括 prompt 规范、code review 策略等。
5. CodeBuddy、Cursor、Claude Code 三者你都用过吗？各自的定位差异是什么？举一个你用 CodeBuddy 解决实际问题的案例。
6. 你如何根据任务类型选择不同的 AI Coding 工具？比如大规模重构用哪个、快速原型用哪个、debug 用哪个——你的选择框架是什么？
7. 最近半年你跟进的印象最深的一个 AI 新协议或新模型能力是什么？它如何影响了你的技术决策或产品设计？
8. 你日常通过什么渠道跟进 AI 前沿？当一个新的协议/模型发布时，你的评估框架是什么——怎么判断它值不值得投入精力？

> 准备方向：要有具体的工作流描述，能说出 before/after 的效率差异，而非泛泛谈工具好用。如果没用过 CodeBuddy，至少了解它是百度出的 AI 编程助手，能说出它和 Cursor（IDE 集成、tab 补全为主）与 Claude Code（CLI、Agent 模式为主）的定位差异。新协议/新模型要准备 1-2 个具体案例，能说出"它改变了我的某个设计决策"。

---

## 七、端到端系统设计与项目经历

1. 描述你主导过的最复杂的一个全栈项目，架构是什么，你负责哪些部分，最大挑战是什么？
2. 从零设计一个 AI 创作工具的后端架构，如何考虑任务队列、状态管理和 AI 调用的解耦？
3. 你如何做 AI 应用的性能优化？包括首 token 延迟、流式渲染体验、并发控制等。
4. 在同时负责多条产品线的情况下，你如何做技术决策的优先级排序？
5. 你对 WorkRally 这类 AI 创作工具有什么产品层面的看法？现有产品有哪些可以改进的地方？
6. 你同时负责 Web/桌面/移动三条产品线，核心业务逻辑层的跨端复用策略是什么？共享层和平台适配层的边界怎么划？
7. 在资源有限的小团队里，你如何决定一个功能是"三端同时做"还是"先在一端验证再铺开"？举一个你做过的取舍案例。

> 准备方向：系统设计题要能画图，能讲清楚数据流；最后一题是产品品味考察，建议提前真正使用过他们的产品再来回答。面试官想听的是工程效率思维，要能讲清楚 monorepo 里的包划分策略（core / web / desktop / mobile），以及共享逻辑层如何通过抽象隔离平台差异。